export default function HomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>My Gemini API Endpoint is Ready!</h1>
      <p>
        Send a POST request to <code>/api/gemini</code> to use it.
      </p>
    </main>
  );
}
